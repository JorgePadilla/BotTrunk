en:
  activerecord:
    attributes:
      seller_inquiry:
        email: "Email"
        service_name: "Service name"
        upstream_url: "Upstream URL"
        price_atomic: "Price per call"
        notes: "Notes"
    errors:
      models:
        seller_inquiry:
          attributes:
            price_atomic:
              blank: "must be a number in USDC, like 0.05"
              greater_than: "must be greater than 0"
