#!/usr/bin/env bash
# Create/inspect the machine-local TestNet payer wallet, or `bin/agent-wallet optin`.
set -euo pipefail
cd "$(dirname "$0")/.."
PY="$(bin/python-env)"
exec "$PY" script/agent_wallet.py "$@"
