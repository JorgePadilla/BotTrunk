<div role="tablist" class="tabs tabs-border">
  <% tabs.each do |label, href| %>
    <%= tag.a label, role: "tab", href: href, class: tab_classes(label), "aria-selected": (true if active?(label)) %>
  <% end %>
</div>
