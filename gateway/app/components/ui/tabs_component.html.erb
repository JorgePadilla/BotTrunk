<div role="tablist" class="tabs tabs-border">
  <% @tabs.each do |tab| %>
    <a role="tab" class="<%= tab_classes(tab) %>" <%= "aria-selected=true" if active?(tab) %>><%= tab %></a>
  <% end %>
</div>
